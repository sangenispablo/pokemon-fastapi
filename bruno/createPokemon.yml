info:
  name: createPokemon
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:8000/pokemon
  body:
    type: json
    data: |-
      {
        "number": 14,
        "name": "ReBulbasaur"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​